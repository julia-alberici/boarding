model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  name      String
  boards    Board[]
  tasks     Task[]
  createdAt DateTime @default(now())
}
